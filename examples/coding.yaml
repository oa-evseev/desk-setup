version: 1

monitors:

  left:
    windows:

      - exec:
          - firefox
          - --new-window
          - https://app.todoist.com/app/today
        tile: left
        after:
          - exec:
              - firefox
              - --new-tab
              - https://chatgpt.com

      - exec:
          - konsole
        tile: right

  center:
    windows:

      - exec:
          - kate
        cwd: ~/projects/desk-setup
        tile: left

      - exec:
          - konsole
        cwd: ~/projects/desk-setup
        tile: right

  right:
    windows: []
